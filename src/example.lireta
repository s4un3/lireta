config "path/to/config.json";
section A {
    # foo
    # bar
    seq A B C D E
        F G A B C;
}

voice std.sin {
	instrument other;
	sfx effect;

	if thing {
	   /* some stuff
		that might be relevant
		or might not*/
	}
}
